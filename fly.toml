app = "diayquien"
primary_region = "mia"  # Miami — closest to Costa Rica

[build]

[env]
  PYTHONUNBUFFERED = "1"

# This is a polling bot — no HTTP service needed.
# Fly will keep the process alive as a background worker.
[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
